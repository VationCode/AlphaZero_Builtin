using UnityEngine;

// 수영 State Flow 구현을 확장하기 위한 현재 플레이스홀더다.
public class SwimStateFlow : MonoBehaviour
{
    // 현재 초기화 로직은 없다.
    void Start()
    {
        
    }

    // 현재 프레임 갱신 로직은 없다.
    void Update()
    {
        
    }
}
