using UnityEngine;

// 무기 행동 간 공유 상태를 확장하기 위한 현재 플레이스홀더다.
public class WeaponActionContext : MonoBehaviour
{
    // 현재 초기화 로직은 없다.
    void Start()
    {
        
    }

    // 현재 프레임 갱신 로직은 없다.
    void Update()
    {
        
    }
}
