using UnityEngine;

// Sniper 전용 동작을 확장하기 위한 현재 플레이스홀더다.
public class Sniper : MonoBehaviour
{
    // 현재 초기화 로직은 없다.
    void Start()
    {
        
    }

    // 현재 프레임 갱신 로직은 없다.
    void Update()
    {
        
    }
}
